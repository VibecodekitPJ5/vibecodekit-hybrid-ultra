# Portfolio (VibecodeKit scaffold)

Generated by `vibecodekit.scaffold_engine` — preset `portfolio`, stack `nextjs`.

## Run

```bash
npm install
npm run dev
```

## Sections

- `components/Hero.tsx` — opening hero (Framer Motion fade-in)
- `components/Work.tsx` — recent work grid (3 placeholder projects)
- `components/Contact.tsx` — email + GitHub links

## What to edit

- Replace "Tên Bạn" / placeholder copy in `components/Hero.tsx`.
- Update the `projects` array in `components/Work.tsx`.
- Swap the contact targets in `components/Contact.tsx`.

Vietnamese-first by default — `lang="vi"` is set on `<html>`.
