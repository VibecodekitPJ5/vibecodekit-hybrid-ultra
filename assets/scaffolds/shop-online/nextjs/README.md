# Shop Online (VibecodeKit scaffold)

Catalog + cart skeleton.  Wire your payment provider (Polar, Stripe, VNPay) in
`app/products/page.tsx` and `app/checkout/page.tsx`.

```bash
npm install
npm run dev
```
