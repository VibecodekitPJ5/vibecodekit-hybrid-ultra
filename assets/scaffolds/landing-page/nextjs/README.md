# Landing Page (VibecodeKit scaffold)

Generated by `vibecodekit.scaffold_engine` — preset `landing-page`, stack `nextjs`.

## Run

```bash
npm install
npm run dev
```

Open <http://localhost:3000>.

## Structure

- `app/page.tsx` — hero + features + email capture
- `app/layout.tsx` — root layout
- `app/globals.css` — Tailwind directives

## Next steps

Wire the email-capture POST to your provider (Supabase, Resend, ConvertKit).
